spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/medlibrary
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

security:
  jwt:
    issuer: medlibrary
    secret: ${JWT_SECRET}
    expiration: 3600
